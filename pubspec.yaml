name: flutter_hashicon
description: Hashicon is a unique avatar generator that creates a beautiful icon based on any string you provide.
version: 0.0.2
homepage:

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  hashlib: ^1.21.0
  image: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
